// examples/showcase.sat - Showcase of Satori's current features

import io

io.println "╔═══════════════════════════════════════════╗"
io.println "║     SATORI PROGRAMMING LANGUAGE           ║"
io.println "║         Feature Showcase                  ║"
io.println "╚═══════════════════════════════════════════╝"
io.println ""

// Variables and arithmetic
io.println "[ Arithmetic & Variables ]"
let x := 42
let y := 7
io.println "x = {}, y = {}", x, y
io.println "x + y = {}", x + y
io.println "x * y = {}", x * y
io.println "x / y = {}", x / y
io.println "x % y = {}", x % y
io.println ""

// Operator precedence
io.println "[ Operator Precedence ]"
let expr := 2 + 3 * 4
io.println "2 + 3 * 4 = {}", expr
io.println "(multiplication before addition)"
io.println ""

// Comparisons
io.println "[ Comparisons ]"
io.println "42 > 7  = {}", 42 > 7
io.println "42 == 42 = {}", 42 == 42
io.println "5 != 10 = {}", 5 != 10
io.println ""

// Control flow
io.println "[ Control Flow ]"
let temp := 25

if temp > 30 then
    io.println "It's hot!"
else
    if temp > 20 then
        io.println "It's nice!"
    else
        io.println "It's cold!"
        
io.println ""

// Practical example
io.println "[ Practical Example: Grade Calculator ]"
let student_score := 88

io.print "Score {}: ", student_score
if student_score >= 90 then
    io.println "A - Excellent!"
else
    if student_score >= 80 then
        io.println "B - Good!"
    else
        if student_score >= 70 then
            io.println "C - Average"
        else
            io.println "F - Needs Improvement"

io.println ""
io.println "╔═══════════════════════════════════════════╗"
io.println "║   Satori - Clean. Fast. Explicit. 🌸     ║"
io.println "╚═══════════════════════════════════════════╝"
